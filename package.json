{
  "name": "tictactoe-eth",
  "description": "Play tic tac toe - with Solidity",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "author": "Gianluca Casati (http://fibo.github.io/)",
  "scripts": {
    "check": "npm run hardhat:check",
    "clean": "npm run hardhat:clean",
    "deploy:contract:localhost": "hardhat run scripts/deploy.ts --network localhost",
    "hardhat": "hardhat",
    "hardhat:check": "hardhat check",
    "hardhat:clean": "hardhat clean",
    "hardhat:compile": "hardhat compile",
    "hardhat:node": "hardhat node",
    "hardhat:test": "hardhat test",
    "prehardhat:check": "npm run tsc--noEmit",
    "prehardhat:compile": "npm run hardhat:clean",
    "pretsc--noEmit": "npm run hardhat:compile",
    "pretest": "npm run check",
    "test": "npm run hardhat:test",
    "tsc--noEmit": "tsc --noEmit --project ."
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "dot-editorconfig": "^1.2.0",
    "hardhat": "^2.10.1",
    "typescript": "^4.7.4"
  }
}
